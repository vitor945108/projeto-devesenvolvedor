import './cs.css';
import Logo from '../../imagens/logo.png';
import Head from '../head/Head';
import Voltarhome from '../Voltarhome/Voltahome';

function Cs(){
          return(
 <div className='so'>
            <Head titulo="css"/> 
 <div><h2>CSS é uma linguagem de estilo que define a apresentação de um documento escrito em HTML ou em XML123. O CSS separa o conteúdo da representação visual do site, permitindo controlar o design, layout, fonte e a cor dos elementos34. Uma regra CSS é composta por um seletor, que identifica o elemento HTML, e uma propriedade, que define o valor da estilização5</h2></div> 
         <Voltarhome></Voltarhome>
          </div>
          )
}

export default Cs;